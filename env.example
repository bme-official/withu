## Server-only env (Vercel Project Settings -> Environment Variables)
SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# optional (default: gpt-4o-mini)
OPENAI_CHAT_MODEL="gpt-4o-mini"

# optional: comma-separated origins allowed to call /api/* from embedded sites (CORS)
# if unset, APIs respond with Access-Control-Allow-Origin: *
WIDGET_ALLOWED_ORIGINS="https://mirai-aizawa.com"

# optional: OpenAI TTS model for /api/tts (server-side mp3). Default: tts-1
# default is tts-1 (lower latency). For higher quality, set tts-1-hd.
OPENAI_TTS_MODEL="tts-1"

# optional: OpenAI TTS voice (alloy|echo|fable|onyx|nova|shimmer). Default: shimmer
OPENAI_TTS_VOICE="shimmer"

# optional: enable additional AI call for intimacy scoring (slower). Default: off.
WITHU_AI_INTIMACY="0"

# --- Admin UI (optional) ---
# Set these to enable /admin (password-protected).
ADMIN_PASSWORD="change-me"
ADMIN_SESSION_SECRET="change-me-too-please-use-a-long-random-string"


